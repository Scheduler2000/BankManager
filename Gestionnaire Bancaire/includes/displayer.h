#ifndef __MENU__
#define __MENU__
#include "../includes/list.h"

void DisplayMenu(t_list* bank); /* Affiche un menu sur la console avec les differentes options disponibles. */
static void DisplayFeatures(); /* Affiche les fonctionnalitées présentes sur la console. */
#endif
